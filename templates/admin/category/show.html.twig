{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block stylesheets %}
<style>
.inline {
    display:inline-block !important;
}
</style>
{% endblock %}

{% block body %}
    <h1>Information Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ category.name }}</td>
            </tr>
            <tr>
                <th>Action</th>
                <td><a href="{{ path('admin_category_edit', {'id': category.id}) }}"class="btn btn-primary inline">Modifier</a> {{ include('admin/category/_delete_form.html.twig') }}</td>
            </tr>
        </tbody>
    </table>

<a href="{{ path('admin_category_index') }}">Revenir à la liste</a>
{% endblock %}
